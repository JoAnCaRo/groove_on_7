// Importa funciones y estilos necesarios
import { smoothScroll } from './utils/animations.js';

document.addEventListener('DOMContentLoaded', () => {
  console.log('DOM completamente cargado y procesado');
  smoothScroll();
});
